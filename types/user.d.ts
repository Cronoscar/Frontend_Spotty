export type User = {
		fullName: string;
		email: string;
		bookings: number;
		expenses: number;
		favorites: number;
};

export type UserData = {
		name: string;
		lastName: string;
		phoneNumber: string;
		email: string;
}
